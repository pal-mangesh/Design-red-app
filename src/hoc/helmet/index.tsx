import React, { ReactElement } from 'react'

interface Props {
    
}

function HelmetContainer({}: Props): ReactElement {
    return (
        <div>
            
        </div>
    )
}

export default HelmetContainer
